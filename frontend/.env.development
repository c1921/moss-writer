# 开发环境：前端在 localhost:5173，后端在 localhost:8080
# 使用 Vite 代理（vite.config.ts 中配置），前端可直接用 /api 和 /ws 路径
# 如不使用代理可取消注释以下两行：
# VITE_API_BASE_URL=http://localhost:8080/api
# VITE_WS_URL=ws://localhost:8080/ws
